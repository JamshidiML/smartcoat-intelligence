class EmbeddingService:
    """Placeholder embedding service for future vector search."""

    def embed(self, text: str) -> list[float]:
        if not text:
            return []
        return [0.0]
